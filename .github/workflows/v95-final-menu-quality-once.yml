name: V95 Final Menu Cleanup Quality Once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v95-final-menu-quality-once.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Frontend quality
        run: npm run lint
      - name: API TypeScript quality
        run: npx tsc --noEmit -p tsconfig.api.json
      - name: Production build
        run: npm run build
      - name: Verify final settings UX
        run: |
          grep -q "label:'Site Ayarları'" src/pages/admin/admin-layout.component.ts
          grep -q "queryParams: { section }" src/pages/admin/admin-site-settings-hub.component.ts
          ! grep -q "openAdminMenu" src/pages/admin/admin-site-settings-hub.component.ts
          ! grep -q "Canlı site_config" src/pages/admin/admin-settings.component.ts
          git diff --check
      - name: Record final verification
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/v95-final-menu-quality-once.yml
          git commit -m "V95: verify final site settings menu cleanup"
          git push origin main
