import fs from 'node:fs';

function read(path){ return fs.readFileSync(path,'utf8'); }
function write(path, value){ fs.writeFileSync(path,value); }
function replaceOnce(path, from, to){
  const source=read(path);
  if(!source.includes(from)) throw new Error(`Target not found in ${path}: ${from.slice(0,120)}`);
  write(path, source.replace(from,to));
}
function replaceBetween(path, start, end, replacement){
  const source=read(path);
  const a=source.indexOf(start); if(a<0) throw new Error(`Start not found in ${path}: ${start}`);
  const b=source.indexOf(end,a+start.length); if(b<0) throw new Error(`End not found in ${path}: ${end}`);
  write(path, source.slice(0,a)+replacement+source.slice(b));
}

// 1) Date control: one permanent TalkBack name. aria-labelledby was overriding aria-label.
replaceOnce('src/components/accessible-native-date.component.ts',
  '        [attr.aria-labelledby]="labelId"\n        [attr.aria-describedby]="value ? valueId : null"\n        [attr.aria-label]="buttonAccessibleName()"',
  '        [attr.aria-describedby]="value ? valueId : null"\n        [attr.aria-label]="buttonAccessibleName()"\n        [attr.title]="buttonAccessibleName()"');
replaceOnce('src/components/accessible-native-date.component.ts',
  '    return `${this.normalizedLabel()} için tarihi seç${selected}`;',
  '    return `${this.normalizedLabel()}: Tarihi seç${selected}`;');

// 2) Site config contract for a database-driven planner.
replaceOnce('src/models/site-config.model.ts',
  '    plannerKicker?: string;\n    plannerNote?: string;',
  `    plannerKicker?: string;\n    plannerNote?: string;\n    plannerEnabled?: boolean;\n    plannerVariant?: 'classic' | 'compact';\n    plannerFieldOrder?: Array<'service' | 'pickup' | 'duration' | 'date'>;\n    plannerServiceOptions?: Array<{ value: 'individual' | 'driver' | 'wedding' | 'tour'; label: string; enabled?: boolean; sortOrder?: number }>;\n    plannerDurationOptions?: Array<{ value: 'hourly' | 'daily' | 'weekly' | 'monthly' | 'longterm'; label: string; enabled?: boolean; sortOrder?: number }>;`);

// 3) Public planner: explicit permanent labels, correct order and admin-driven configuration.
replaceOnce('src/pages/home-v71.component.ts',
  'type PlannerService = "individual" | "driver" | "wedding" | "tour";',
  `type PlannerService = "individual" | "driver" | "wedding" | "tour";\ntype PlannerFieldKey = "service" | "pickup" | "duration" | "date";\ninterface PlannerServiceChoice { value: PlannerService; label: string; enabled: boolean; sortOrder: number; }\ninterface PlannerDurationChoice { value: RentalDuration; label: string; enabled: boolean; sortOrder: number; }`);
replaceOnce('src/pages/home-v71.component.ts','        <div class="hero-stage">','        <div class="hero-stage" [class.planner-disabled]="!plannerEnabled()">');
replaceOnce('src/pages/home-v71.component.ts',
  '<div class="search-shell"><mat-icon aria-hidden="true">search</mat-icon><input id="home-search-v80" type="search" [(ngModel)]="searchQuery" (keyup.enter)="performSearch()" autocomplete="off" [placeholder]="homeContent().searchPlaceholder || \'Marka, model, tur veya ilan no\'" /><button type="button" (click)="performSearch()">{{ homeContent().searchButtonLabel || \'Ara\' }}</button></div>',
  '<div class="search-shell"><mat-icon aria-hidden="true">search</mat-icon><input id="home-search-v80" type="search" [(ngModel)]="searchQuery" (keyup.enter)="performSearch()" autocomplete="off" aria-label="Araç, tur veya ilan ara" [placeholder]="homeContent().searchPlaceholder || \'Marka, model, tur veya ilan no\'" /><button type="button" (click)="performSearch()" aria-label="Aramayı başlat">{{ homeContent().searchButtonLabel || \'Ara\' }}</button></div>');
replaceOnce('src/pages/home-v71.component.ts',
  '          <aside class="planner" aria-labelledby="planner-title">',
  '          @if (plannerEnabled()) {\n          <aside class="planner" [class.planner-compact]="plannerVariant() === \'compact\'" aria-labelledby="planner-title">');
replaceOnce('src/pages/home-v71.component.ts',
  '            <button type="button" class="planner-action" (click)="searchAvailability()"><span>{{ bookingButtonLabel() }}</span><mat-icon aria-hidden="true">arrow_forward</mat-icon></button>',
  '            <button type="button" class="planner-action" (click)="searchAvailability()" [attr.aria-label]="bookingButtonLabel()"><span>{{ bookingButtonLabel() }}</span><mat-icon aria-hidden="true">arrow_forward</mat-icon></button>');
replaceOnce('src/pages/home-v71.component.ts',
  '            <p class="planner-note">{{ homeContent().plannerNote || \'Kesin rezervasyon, araç ve seçilen zaman aralığı uygunluğu doğrulandıktan sonra oluşur.\' }}</p>\n          </aside>\n        </div>',
  '            <p class="planner-note">{{ homeContent().plannerNote || \'Kesin rezervasyon, araç ve seçilen zaman aralığı uygunluğu doğrulandıktan sonra oluşur.\' }}</p>\n          </aside>\n          }\n        </div>');

const plannerFields = String.raw`            <div class="field-grid">
              @for (field of plannerFieldOrder(); track field) {
                @switch (field) {
                  @case ('service') {
                    <label class="field"><span>{{ homeContent().plannerServiceLabel || 'Ne için araç veya hizmet arıyorsunuz?' }}</span><select [(ngModel)]="serviceType" name="homeService" (ngModelChange)="onServiceChanged()" [attr.aria-label]="homeContent().plannerServiceLabel || 'Ne için araç veya hizmet arıyorsunuz?'">@for (option of plannerServiceOptions(); track option.value) {<option [value]="option.value">{{ option.label }}</option>}</select></label>
                  }
                  @case ('pickup') {
                    @if (serviceType !== 'tour') {
                      <label class="field"><span>{{ homeContent().plannerPickupLabel || 'Nereden teslim almak istiyorsunuz?' }}</span><select [(ngModel)]="selectedPickupKey" name="homePickup" (ngModelChange)="clearPlannerError()" [attr.aria-label]="homeContent().plannerPickupLabel || 'Teslim alma noktasını seç'"><option value="">{{ homeContent().plannerPickupPlaceholder || 'Teslim noktasını seçin' }}</option>@for (choice of pickupChoices(); track choice.key) { <option [value]="choice.key">{{ choice.label }}</option> }</select></label>
                    }
                  }
                  @case ('duration') {
                    @if (serviceType !== 'tour') {
                      <label class="field"><span>Kiralama Süresi</span><select [(ngModel)]="rentalDuration" name="homeDuration" (ngModelChange)="onDurationChanged()" aria-label="Kiralama süresini seç">@for (option of plannerDurationOptions(); track option.value) {<option [value]="option.value">{{ option.label }}</option>}</select></label>
                    }
                  }
                  @case ('date') {
                    @if (serviceType === 'tour') {
                      <div class="date-grid single-date"><app-accessible-native-date [label]="homeContent().plannerTourDateLabel || 'Tur tarihi'" [value]="startDate" [min]="today" (valueChange)="onStartDateChanged($event)" /></div>
                    } @else if (rentalDuration === 'hourly') {
                      <div class="date-grid single-date"><app-accessible-native-date label="Kiralama tarihi" [value]="startDate" [min]="today" (valueChange)="onStartDateChanged($event)" /></div>
                      <div class="time-grid"><label class="field"><span>Alış Saati</span><input type="time" [(ngModel)]="startTime" name="homeStartTime" step="900" (ngModelChange)="clearPlannerError()" aria-label="Alış saatini seç" /></label><label class="field"><span>İade Saati</span><input type="time" [(ngModel)]="endTime" name="homeEndTime" step="900" (ngModelChange)="clearPlannerError()" aria-label="İade saatini seç" /></label></div>
                    } @else {
                      <div class="date-grid"><app-accessible-native-date [label]="homeContent().plannerStartDateLabel || 'Alış tarihi'" [value]="startDate" [min]="today" (valueChange)="onStartDateChanged($event)" /><app-accessible-native-date [label]="homeContent().plannerEndDateLabel || 'İade tarihi'" [value]="endDate" [min]="startDate || today" (valueChange)="onEndDateChanged($event)" /></div>
                    }
                  }
                }
              }
            </div>

`;
replaceBetween('src/pages/home-v71.component.ts','            <div class="field-grid">','            @if (plannerError)',plannerFields);

replaceOnce('src/pages/home-v71.component.ts',
  '  searchQuery="";startDate="";endDate="";startTime="09:00";endTime="10:00";rentalDuration:RentalDuration="daily";serviceType:PlannerService="individual";selectedPickupKey="";plannerError="";readonly today=this.toDateInput(new Date());',
  `  searchQuery="";startDate="";endDate="";startTime="09:00";endTime="10:00";rentalDuration:RentalDuration="daily";serviceType:PlannerService="individual";selectedPickupKey="";plannerError="";readonly today=this.toDateInput(new Date());\n  readonly plannerEnabled=computed(()=>this.homeContent().plannerEnabled!==false);\n  readonly plannerVariant=computed<"classic"|"compact">(()=>this.homeContent().plannerVariant==="compact"?"compact":"classic");\n  readonly plannerFieldOrder=computed<PlannerFieldKey[]>(()=>{const defaults:PlannerFieldKey[]=["service","pickup","duration","date"];const raw=Array.isArray(this.homeContent().plannerFieldOrder)?this.homeContent().plannerFieldOrder!:[];const result:PlannerFieldKey[]=[];for(const value of raw){if(defaults.includes(value as PlannerFieldKey)&&!result.includes(value as PlannerFieldKey))result.push(value as PlannerFieldKey);}for(const value of defaults)if(!result.includes(value))result.push(value);return result;});\n  readonly plannerServiceOptions=computed<PlannerServiceChoice[]>(()=>{const home=this.homeContent();const defaults:PlannerServiceChoice[]=[{value:"individual",label:home.plannerServiceIndividual||"Şoförsüz araç kiralama",enabled:true,sortOrder:0},{value:"driver",label:home.plannerServiceDriver||"Şoförlü araç / transfer",enabled:true,sortOrder:1},{value:"wedding",label:home.plannerServiceWedding||"Düğün / özel gün aracı",enabled:true,sortOrder:2},{value:"tour",label:home.plannerServiceTour||"Tur / gezi planı",enabled:true,sortOrder:3}];const raw=Array.isArray(home.plannerServiceOptions)?home.plannerServiceOptions:[];const mapped=defaults.map((item,index)=>{const custom=raw.find((row)=>row?.value===item.value);return{...item,label:String(custom?.label||item.label).trim()||item.label,enabled:custom?.enabled!==false,sortOrder:Number.isFinite(Number(custom?.sortOrder))?Number(custom?.sortOrder):index};}).filter((item)=>item.enabled).sort((a,b)=>a.sortOrder-b.sortOrder);return mapped.length?mapped:defaults;});\n  readonly plannerDurationOptions=computed<PlannerDurationChoice[]>(()=>{const defaults:PlannerDurationChoice[]=[{value:"hourly",label:"Saatlik",enabled:true,sortOrder:0},{value:"daily",label:"Günlük",enabled:true,sortOrder:1},{value:"weekly",label:"Haftalık",enabled:true,sortOrder:2},{value:"monthly",label:"Aylık",enabled:true,sortOrder:3},{value:"longterm",label:"Uzun Süre",enabled:true,sortOrder:4}];const raw=Array.isArray(this.homeContent().plannerDurationOptions)?this.homeContent().plannerDurationOptions!:[];const mapped=defaults.map((item,index)=>{const custom=raw.find((row)=>row?.value===item.value);return{...item,label:String(custom?.label||item.label).trim()||item.label,enabled:custom?.enabled!==false,sortOrder:Number.isFinite(Number(custom?.sortOrder))?Number(custom?.sortOrder):index};}).filter((item)=>item.enabled).sort((a,b)=>a.sortOrder-b.sortOrder);return mapped.length?mapped:defaults;});`);
replaceOnce('src/pages/home-v71.component.ts',
  '  constructor(){void this.homepageLayout.load();void this.branchService.refresh();void this.campaignService.loadPublic().catch(()=>undefined);effect(()=>{const cfg=this.config();this.seo.updateSeoTags({title:cfg.seoTitle||`${cfg.companyName} | Araç Kiralama, Satış ve Turlar`,description:cfg.seoDescription||this.homeContent().heroSubtitle||cfg.companyName,keywords:cfg.seoKeywords,image:cfg.seoOgImage||cfg.logoUrl||this.fallbackHero});});}',
  '  constructor(){void this.homepageLayout.load();void this.branchService.refresh();void this.campaignService.loadPublic().catch(()=>undefined);effect(()=>{const cfg=this.config();this.seo.updateSeoTags({title:cfg.seoTitle||`${cfg.companyName} | Araç Kiralama, Satış ve Turlar`,description:cfg.seoDescription||this.homeContent().heroSubtitle||cfg.companyName,keywords:cfg.seoKeywords,image:cfg.seoOgImage||cfg.logoUrl||this.fallbackHero});});effect(()=>{const services=this.plannerServiceOptions();if(!services.some((item)=>item.value===this.serviceType))this.serviceType=services[0]?.value||"individual";const durations=this.plannerDurationOptions();if(!durations.some((item)=>item.value===this.rentalDuration))this.rentalDuration=durations[0]?.value||"daily";});}');
replaceOnce('src/pages/home-v71.component.ts',
  'const duration=this.serviceType==="tour"?"":this.rentalDuration==="hourly"?"Saatlik":this.rentalDuration==="daily"?"Günlük":this.rentalDuration==="monthly"?"Aylık":"Uzun dönem";',
  'const duration=this.serviceType==="tour"?"":(this.plannerDurationOptions().find((item)=>item.value===this.rentalDuration)?.label||this.rentalDuration);');
replaceOnce('src/pages/home-v71.component.ts',
  '@media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;transition:none!important;animation:none!important}}',
  '.planner.planner-compact{padding:.78rem}.planner.planner-compact .field-grid{gap:.52rem}@media(min-width:1024px){.hero-stage.planner-disabled{grid-template-columns:1fr}.hero-stage.planner-disabled .hero-copy-block{max-width:850px}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;transition:none!important;animation:none!important}}');

// 4) Admin: manage planner visibility, version, order and option labels without touching code.
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  "type MediaPurpose = 'profile' | 'cover' | 'background';",
  `type MediaPurpose = 'profile' | 'cover' | 'background';\ntype PlannerFieldKey = 'service' | 'pickup' | 'duration' | 'date';\ntype PlannerVariant = 'classic' | 'compact';\ninterface PlannerServiceAdminOption { value: 'individual' | 'driver' | 'wedding' | 'tour'; label: string; enabled: boolean; sortOrder: number; }\ninterface PlannerDurationAdminOption { value: 'hourly' | 'daily' | 'weekly' | 'monthly' | 'longterm'; label: string; enabled: boolean; sortOrder: number; }`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  '<div><h2 id="homepage-top-area-title">Ana Sayfa Üst Alanı</h2><p>Telefonlarda sade Hero kullanılır: üst satır, başlık, açıklama ve arka plan. Arama ve Hızlı Planlama yalnız tablet ve bilgisayarda gösterilir.</p></div>',
  '<div><h2 id="homepage-top-area-title">Ana Sayfa Üst Alanı</h2><p>Hero ve Hızlı Planlama tüm ekran boyutlarında aynı canlı ayarlardan çalışır. Planlama alanını açıp kapatabilir, görünümünü, alan sırasını ve seçeneklerini burada yönetebilirsiniz.</p></div>');

const adminPlannerControls = String.raw`              <label><span>Planlama alt notu</span><textarea [(ngModel)]="plannerNote" name="homePlannerNote" rows="3" maxlength="300" aria-label="Ana sayfa hızlı planlama alt notu"></textarea></label>
              <label class="visibility-toggle"><input type="checkbox" [(ngModel)]="plannerEnabled" name="homePlannerEnabled" aria-label="Hızlı Planlama bölümünü ana sayfada göster" /><span>Hızlı Planlama bölümünü ana sayfada göster</span></label>
              <label><span>Planlama sürümü / görünümü</span><select [(ngModel)]="plannerVariant" name="homePlannerVariant" aria-label="Hızlı Planlama görünüm sürümünü seç"><option value="classic">Standart</option><option value="compact">Kompakt</option></select></label>
              <fieldset class="planner-admin-box"><legend>Form alanlarının sırası</legend><p class="hint">Varsayılan ve önerilen sıra: Hizmet → Teslim noktası → Kiralama süresi → Tarih ve saat.</p><div class="planner-order-list">@for (field of plannerFieldOrder; track field; let i = $index) {<div class="planner-order-row"><strong>{{ plannerFieldLabel(field) }}</strong><div><button type="button" class="small-button" (click)="movePlannerField(i,-1)" [disabled]="i===0" [attr.aria-label]="plannerFieldLabel(field) + ' alanını yukarı taşı'">↑</button><button type="button" class="small-button" (click)="movePlannerField(i,1)" [disabled]="i===plannerFieldOrder.length-1" [attr.aria-label]="plannerFieldLabel(field) + ' alanını aşağı taşı'">↓</button></div></div>}</div></fieldset>
              <fieldset class="planner-admin-box"><legend>Hizmet seçenekleri</legend><div class="planner-option-list">@for (option of plannerServiceOptions; track option.value; let i = $index) {<div class="planner-option-row"><label class="mini-toggle"><input type="checkbox" [(ngModel)]="option.enabled" [name]="'planner-service-enabled-' + option.value" [attr.aria-label]="option.label + ' hizmet seçeneğini etkinleştir'" /> Aktif</label><input [(ngModel)]="option.label" [name]="'planner-service-label-' + option.value" maxlength="80" [attr.aria-label]="option.value + ' hizmet seçeneği metni'" /><div><button type="button" class="small-button" (click)="movePlannerServiceOption(i,-1)" [disabled]="i===0" [attr.aria-label]="option.label + ' hizmet seçeneğini yukarı taşı'">↑</button><button type="button" class="small-button" (click)="movePlannerServiceOption(i,1)" [disabled]="i===plannerServiceOptions.length-1" [attr.aria-label]="option.label + ' hizmet seçeneğini aşağı taşı'">↓</button></div></div>}</div></fieldset>
              <fieldset class="planner-admin-box"><legend>Kiralama süresi seçenekleri</legend><div class="planner-option-list">@for (option of plannerDurationOptions; track option.value; let i = $index) {<div class="planner-option-row"><label class="mini-toggle"><input type="checkbox" [(ngModel)]="option.enabled" [name]="'planner-duration-enabled-' + option.value" [attr.aria-label]="option.label + ' kiralama süresini etkinleştir'" /> Aktif</label><input [(ngModel)]="option.label" [name]="'planner-duration-label-' + option.value" maxlength="50" [attr.aria-label]="option.value + ' kiralama süresi metni'" /><div><button type="button" class="small-button" (click)="movePlannerDurationOption(i,-1)" [disabled]="i===0" [attr.aria-label]="option.label + ' kiralama süresini yukarı taşı'">↑</button><button type="button" class="small-button" (click)="movePlannerDurationOption(i,1)" [disabled]="i===plannerDurationOptions.length-1" [attr.aria-label]="option.label + ' kiralama süresini aşağı taşı'">↓</button></div></div>}</div></fieldset>`;
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  '              <label><span>Planlama alt notu</span><textarea [(ngModel)]="plannerNote" name="homePlannerNote" rows="3" maxlength="300" aria-label="Ana sayfa hızlı planlama alt notu"></textarea></label>',
  adminPlannerControls);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  "  plannerNote = '';\n  trustPrice = '';",
  `  plannerNote = '';\n  plannerEnabled = true;\n  plannerVariant: PlannerVariant = 'classic';\n  plannerFieldOrder: PlannerFieldKey[] = ['service','pickup','duration','date'];\n  plannerServiceOptions: PlannerServiceAdminOption[] = [\n    { value:'individual', label:'Şoförsüz araç kiralama', enabled:true, sortOrder:0 },\n    { value:'driver', label:'Şoförlü araç / transfer', enabled:true, sortOrder:1 },\n    { value:'wedding', label:'Düğün / özel gün aracı', enabled:true, sortOrder:2 },\n    { value:'tour', label:'Tur / gezi planı', enabled:true, sortOrder:3 },\n  ];\n  plannerDurationOptions: PlannerDurationAdminOption[] = [\n    { value:'hourly', label:'Saatlik', enabled:true, sortOrder:0 },\n    { value:'daily', label:'Günlük', enabled:true, sortOrder:1 },\n    { value:'weekly', label:'Haftalık', enabled:true, sortOrder:2 },\n    { value:'monthly', label:'Aylık', enabled:true, sortOrder:3 },\n    { value:'longterm', label:'Uzun Süre', enabled:true, sortOrder:4 },\n  ];\n  trustPrice = '';`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  "      homeContent['plannerNote'] = this.plannerNote.trim();\n      homeContent['trustPrice'] = this.trustPrice.trim();",
  `      homeContent['plannerNote'] = this.plannerNote.trim();\n      homeContent['plannerEnabled'] = this.plannerEnabled;\n      homeContent['plannerVariant'] = this.plannerVariant;\n      homeContent['plannerFieldOrder'] = [...this.plannerFieldOrder];\n      homeContent['plannerServiceOptions'] = this.plannerServiceOptions.map((item,index)=>({ ...item, label:item.label.trim(), sortOrder:index }));\n      homeContent['plannerDurationOptions'] = this.plannerDurationOptions.map((item,index)=>({ ...item, label:item.label.trim(), sortOrder:index }));\n      homeContent['trustPrice'] = this.trustPrice.trim();`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  "    this.plannerNote = String(home['plannerNote'] || '');\n    this.trustPrice = String(home['trustPrice'] || '');",
  `    this.plannerNote = String(home['plannerNote'] || '');\n    this.plannerEnabled = home['plannerEnabled'] !== false;\n    this.plannerVariant = home['plannerVariant'] === 'compact' ? 'compact' : 'classic';\n    this.plannerFieldOrder = this.normalizePlannerFieldOrder(home['plannerFieldOrder']);\n    this.plannerServiceOptions = this.normalizePlannerServiceOptions(home['plannerServiceOptions']);\n    this.plannerDurationOptions = this.normalizePlannerDurationOptions(home['plannerDurationOptions']);\n    this.trustPrice = String(home['trustPrice'] || '');`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  '  async createSection(): Promise<void> {',
  `  movePlannerField(index: number, delta: number): void { const target=index+delta;if(target<0||target>=this.plannerFieldOrder.length)return;[this.plannerFieldOrder[index],this.plannerFieldOrder[target]]=[this.plannerFieldOrder[target],this.plannerFieldOrder[index]]; }\n  movePlannerServiceOption(index: number, delta: number): void { const target=index+delta;if(target<0||target>=this.plannerServiceOptions.length)return;[this.plannerServiceOptions[index],this.plannerServiceOptions[target]]=[this.plannerServiceOptions[target],this.plannerServiceOptions[index]]; }\n  movePlannerDurationOption(index: number, delta: number): void { const target=index+delta;if(target<0||target>=this.plannerDurationOptions.length)return;[this.plannerDurationOptions[index],this.plannerDurationOptions[target]]=[this.plannerDurationOptions[target],this.plannerDurationOptions[index]]; }\n  plannerFieldLabel(field: PlannerFieldKey): string { return ({service:'Hizmet türü',pickup:'Teslim noktası',duration:'Kiralama süresi',date:'Tarih ve saat'} as Record<PlannerFieldKey,string>)[field]; }\n\n  async createSection(): Promise<void> {`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  '  private message(error: unknown): string { return error instanceof Error ? error.message : \'İşlem tamamlanamadı.\'; }',
  `  private normalizePlannerFieldOrder(value: unknown): PlannerFieldKey[] { const defaults:PlannerFieldKey[]=['service','pickup','duration','date'];const raw=Array.isArray(value)?value:[];const result:PlannerFieldKey[]=[];for(const item of raw){if(defaults.includes(item as PlannerFieldKey)&&!result.includes(item as PlannerFieldKey))result.push(item as PlannerFieldKey);}for(const item of defaults)if(!result.includes(item))result.push(item);return result; }\n  private normalizePlannerServiceOptions(value: unknown): PlannerServiceAdminOption[] { const defaults:PlannerServiceAdminOption[]=[{value:'individual',label:'Şoförsüz araç kiralama',enabled:true,sortOrder:0},{value:'driver',label:'Şoförlü araç / transfer',enabled:true,sortOrder:1},{value:'wedding',label:'Düğün / özel gün aracı',enabled:true,sortOrder:2},{value:'tour',label:'Tur / gezi planı',enabled:true,sortOrder:3}];const raw=Array.isArray(value)?value as Array<Record<string,unknown>>:[];return defaults.map((item,index)=>{const row=raw.find((entry)=>entry?.['value']===item.value);return{...item,label:String(row?.['label']||item.label).trim()||item.label,enabled:row?.['enabled']!==false,sortOrder:Number.isFinite(Number(row?.['sortOrder']))?Number(row?.['sortOrder']):index};}).sort((a,b)=>a.sortOrder-b.sortOrder); }\n  private normalizePlannerDurationOptions(value: unknown): PlannerDurationAdminOption[] { const defaults:PlannerDurationAdminOption[]=[{value:'hourly',label:'Saatlik',enabled:true,sortOrder:0},{value:'daily',label:'Günlük',enabled:true,sortOrder:1},{value:'weekly',label:'Haftalık',enabled:true,sortOrder:2},{value:'monthly',label:'Aylık',enabled:true,sortOrder:3},{value:'longterm',label:'Uzun Süre',enabled:true,sortOrder:4}];const raw=Array.isArray(value)?value as Array<Record<string,unknown>>:[];return defaults.map((item,index)=>{const row=raw.find((entry)=>entry?.['value']===item.value);return{...item,label:String(row?.['label']||item.label).trim()||item.label,enabled:row?.['enabled']!==false,sortOrder:Number.isFinite(Number(row?.['sortOrder']))?Number(row?.['sortOrder']):index};}).sort((a,b)=>a.sortOrder-b.sortOrder); }\n\n  private message(error: unknown): string { return error instanceof Error ? error.message : 'İşlem tamamlanamadı.'; }`);
replaceOnce('src/pages/admin/admin-homepage.component.ts',
  '.alert{border:1px solid #fecdd3;border-radius:14px;background:#fff1f2;padding:.8rem;color:#be123c;font-size:.75rem;font-weight:850}.sr-only',
  '.alert{border:1px solid #fecdd3;border-radius:14px;background:#fff1f2;padding:.8rem;color:#be123c;font-size:.75rem;font-weight:850}.planner-admin-box{margin:.8rem 0 0;border:1px solid #dbe4ef;border-radius:14px;padding:.7rem}.planner-admin-box legend{padding:0 .3rem;color:#334155;font-size:.68rem;font-weight:950}.planner-order-list,.planner-option-list{display:grid;gap:.45rem}.planner-order-row,.planner-option-row{display:flex;align-items:center;gap:.5rem;border:1px solid #e2e8f0;border-radius:11px;background:#f8fafc;padding:.5rem}.planner-order-row>strong{min-width:0;flex:1;font-size:.68rem}.planner-order-row>div,.planner-option-row>div{display:flex;gap:.25rem}.planner-option-row>input{min-width:0;flex:1}.planner-option-row .mini-toggle{margin:0;flex:none}.sr-only');

// 5) CI: prevent a future regression back to unnamed runtime-mutated controls.
replaceOnce('.github/workflows/customer-ops-v150-gate.yml',
  "          grep -Fq \"Saati seç\" src/services/accessibility-runtime.service.ts\n",
  `          grep -Fq \"Saati seç\" src/services/accessibility-runtime.service.ts\n          ! grep -Fq 'AccessibilityRuntimeService' src/app.component.ts\n          grep -Fq '[attr.aria-label]=\"buttonAccessibleName()\"' src/components/accessible-native-date.component.ts\n          ! grep -Fq '[attr.aria-labelledby]=\"labelId\"' src/components/accessible-native-date.component.ts\n          grep -Fq 'aria-label=\"Kiralama süresini seç\"' src/pages/home-v71.component.ts\n          grep -Fq 'aria-label=\"Alış saatini seç\"' src/pages/home-v71.component.ts\n          grep -Fq 'plannerFieldOrder' src/pages/admin/admin-homepage.component.ts\n          grep -Fq 'plannerEnabled' src/pages/admin/admin-homepage.component.ts\n`);

console.log('V154 planner accessibility/admin patch applied.');
