import { CommonModule } from '@angular/common';
import { Component, OnInit, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { CustomerAdminDetailService } from '../../services/customer-admin-detail.service';
import { CustomerDocument } from '../../services/customer-wallet.service';
import { ToastService } from '../../services/toast.service';

@Component({selector:'app-admin-customer-detail',standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`
<main class="page"><header class="top"><div><a routerLink="/admin/customers">← Müşterilere dön</a><p>Müşteri Merkezi</p><h1>{{service.profile()?.full_name || service.profile()?.email || 'Müşteri'}}</h1><span>{{service.profile()?.email}} · {{service.profile()?.phone || 'Telefon yok'}}</span></div><button type="button" (click)="reload()" [disabled]="service.loading()">{{service.loading()?'Yenileniyor…':'Yenile'}}</button></header>
@if(service.loading()){<section class="card loading" role="status">Müşteri verileri hazırlanıyor…</section>}@else if(service.profile();as p){
<section class="stats"><article><small>Seviye</small><strong>{{tierLabel(service.loyalty()?.tier)}}</strong><span>{{service.loyalty()?.points_balance || 0}} puan</span></article><article><small>Başarılı davet</small><strong>{{service.loyalty()?.successful_referrals || 0}}</strong><span>{{service.loyalty()?.referral_points_earned || 0}} davet puanı</span></article><article><small>Belge</small><strong>{{service.documents().length}}</strong><span>{{verifiedDocumentCount()}} doğrulanmış</span></article><article><small>Tamamlanan işlem</small><strong>{{completedCount()}}</strong><span>Kirala, satın al veya tur</span></article></section>
<div class="grid"><section class="card"><header><h2>Profil ve Kolaylık Tercihleri</h2><p>Müşterinin kendi hesabında kaydettiği temel bilgiler.</p></header><dl><div><dt>E-posta</dt><dd>{{p.email||'-'}}</dd></div><div><dt>Telefon</dt><dd>{{p.phone||'-'}}</dd></div><div><dt>Doğum tarihi</dt><dd>{{p.birth_date||'-'}}</dd></div><div><dt>Adres</dt><dd>{{address()}}</dd></div><div><dt>Hızlı işlem</dt><dd>{{service.preferences()?.quick_checkout_enabled===false?'Kapalı':'Açık'}}</dd></div><div><dt>Aylık hedef</dt><dd>@if(service.preferences()?.monthly_spend_target!==null&&service.preferences()?.monthly_spend_target!==undefined){ {{service.preferences()?.monthly_spend_target|number:'1.0-2'}} {{service.preferences()?.preferred_currency}} }@else{ Belirlenmemiş }</dd></div><div><dt>Belge hatırlatma</dt><dd>{{service.preferences()?.document_expiry_reminder_days || 30}} gün önce</dd></div><div><dt>Cüzdan onayı</dt><dd>{{activeConsent()?'Aktif':'Aktif değil'}}</dd></div></dl></section>
<section class="card"><header><h2>Harcama Özeti</h2><p>Yalnız tamamlanmış işlemler, para birimleri birbirine çevrilmeden ayrı gösterilir.</p></header><div class="spend">@for(row of service.spendByCurrency();track row.currency){<article><strong>{{row.spent|number:'1.0-2'}} {{row.currency}}</strong><span>{{row.transactions}} tamamlanan işlem</span><small>{{row.saved|number:'1.0-2'}} {{row.currency}} kampanya/indirim</small></article>}@empty{<p class="empty">Tamamlanmış harcama yok.</p>}</div></section></div>
<section class="card"><header><div><h2>Kimlik ve Ehliyet Kasası</h2><p>Dosyalar private bucket içinde tutulur. “Görüntüle” bağlantısı yalnız kısa süreli imzalı URL üretir.</p></div><span class="badge">{{service.documents().length}} belge</span></header><div class="documents">@for(doc of service.documents();track doc.id){<article><div><strong>{{documentLabel(doc.document_type)}}</strong><span>{{doc.original_name}} · {{fileSize(doc.file_size)}}</span>@if(doc.expiry_date){<small>Geçerlilik: {{doc.expiry_date|date:'dd.MM.yyyy'}}</small>}@if(doc.rejection_reason){<small class="warn">Müşteri notu: {{doc.rejection_reason}}</small>}</div><div class="doc-actions"><b [class.good]="doc.verification_status==='VERIFIED'">{{statusLabel(doc.verification_status)}}</b><button type="button" (click)="viewDocument(doc)">Görüntüle</button><button type="button" (click)="review(doc,'VERIFIED')">Doğrula</button><button type="button" (click)="beginReject(doc)">Yeniden iste</button><button type="button" (click)="review(doc,'EXPIRED')">Süresi doldu</button></div></article>}@empty{<p class="empty">Müşteri henüz belge yüklememiş.</p>}</div>@if(rejecting();as doc){<div class="reject-box"><strong>{{documentLabel(doc.document_type)}} için müşteriye gösterilecek kısa not</strong><input [(ngModel)]="rejectReason" name="rejectReason" aria-label="Belgeyi yeniden yükleme nedeni" placeholder="Örn. Görüntü net değil, lütfen yeniden yükleyin." /><div><button type="button" (click)="rejecting.set(null)">Vazgeç</button><button type="button" class="danger" (click)="confirmReject()" [disabled]="!rejectReason.trim()">Yeniden yükleme iste</button></div></div>}</section>
<section class="grid"><section class="card"><header><h2>Davet Hareketleri</h2><p>Müşterinin gönderdiği ve kendisinin geldiği davet bağlantıları.</p></header><div class="rows">@for(r of service.referrals();track r.id){<article><strong>{{r.inviter_user_id===p.user_id?'Davet ettiği müşteri':'Bu müşteri davetle geldi'}}</strong><span>{{r.referral_code}} · {{r.status}}</span><small>{{r.created_at|date:'dd.MM.yyyy HH:mm'}} · {{r.inviter_points_awarded}} / {{r.invitee_points_awarded}} puan</small></article>}@empty{<p class="empty">Davet kaydı yok.</p>}</div></section>
<section class="card"><header><h2>Davet Ödülleri</h2><p>Aynı arkadaş için her hizmet kategorisinin ilk gerçek tamamlanan işlemi bir kez ödül üretir.</p></header><div class="rows">@for(r of service.referralRewards();track r.id){<article><strong>{{rewardLabel(r.reward_type)}}</strong><span>Davet eden +{{r.inviter_points}} · Yeni müşteri +{{r.invitee_points}}</span><small>{{r.rewarded_at|date:'dd.MM.yyyy HH:mm'}}</small></article>}@empty{<p class="empty">Henüz davet ödülü oluşmamış.</p>}</div></section></section>
<section class="card"><header><h2>Güvenli Ödeme Yöntemleri</h2><p>Admin yalnız kart markası, son dört hane, son kullanım ve durum gibi güvenli metadatayı görür. Tam kart/CVV görünmez.</p></header><div class="rows">@for(card of service.paymentMethods();track card.id){<article><strong>{{card.brand||card.provider}} •••• {{card.last4||'----'}}</strong><span>{{card.status}} {{card.is_default?'· Varsayılan':''}}</span><small>@if(card.expiry_month&&card.expiry_year){Son kullanım {{card.expiry_month}}/{{card.expiry_year}}}</small></article>}@empty{<p class="empty">Kayıtlı ödeme yöntemi yok.</p>}</div></section>
<section class="card"><header><h2>İşlem Geçmişi</h2><p>Rezervasyon, tur ve satış işlemleri.</p></header><div class="rows bookings">@for(b of service.bookings();track b.id){<article><div><strong>{{b.item_name}}</strong><span>{{typeLabel(b.booking_type)}} · {{b.reference}}</span><small>{{b.created_at|date:'dd.MM.yyyy HH:mm'}} · {{b.status}} · {{b.payment_status}}</small></div><div class="money">@if(b.total_price!==null&&b.total_price!==undefined){<strong>{{b.total_price|number:'1.0-2'}} {{b.currency}}</strong>}@if(b.discount_amount){<small>-{{b.discount_amount|number:'1.0-2'}} {{b.currency}} indirim</small>}</div></article>}@empty{<p class="empty">İşlem yok.</p>}</div></section>
<section class="card consent-log"><header><h2>Cüzdan Onay Geçmişi</h2><p>Kabul ve geri çekme kayıtları silinmeden izlenir.</p></header><div class="rows">@for(c of service.consents();track c.terms_version){<article><strong>{{c.terms_version}}</strong><span>Kabul: {{c.accepted_at|date:'dd.MM.yyyy HH:mm'}}</span><small>{{c.revoked_at?('Geri çekildi: '+(c.revoked_at|date:'dd.MM.yyyy HH:mm')):'Aktif onay'}}</small></article>}@empty{<p class="empty">Cüzdan onayı yok.</p>}</div></section>
}</main>`,styles:[`:host{display:block}.page{min-height:100vh;background:#f8fafc;padding:1rem;color:#0f172a}.top,.stats,.grid,.card{max-width:1240px;margin-left:auto;margin-right:auto}.top{display:flex;justify-content:space-between;gap:1rem;align-items:end}.top a{color:#1d4ed8;text-decoration:none;font-weight:850}.top p{margin:.45rem 0 0;color:#64748b;font-size:.62rem;font-weight:950;letter-spacing:.14em;text-transform:uppercase}.top h1{margin:.15rem 0;font-size:1.7rem}.top span{color:#64748b;font-size:.74rem}.top button{min-height:42px;border:0;border-radius:11px;background:#0f172a;color:#fff;padding:0 1rem;font-weight:900}.stats{display:grid;grid-template-columns:repeat(2,1fr);gap:.6rem;margin-top:1rem}.stats article,.card{border:1px solid #e2e8f0;border-radius:17px;background:#fff}.stats article{padding:.85rem}.stats small,.stats strong,.stats span{display:block}.stats small{font-size:.58rem;color:#64748b;text-transform:uppercase;font-weight:900}.stats strong{font-size:1.25rem;margin:.15rem 0}.stats span{font-size:.65rem;color:#64748b}.grid{display:grid;gap:1rem;margin-top:1rem}.card{overflow:hidden;margin-top:1rem}.grid .card{margin-top:0}.card>header{display:flex;justify-content:space-between;gap:1rem;padding:1rem;border-bottom:1px solid #e2e8f0}.card h2{margin:0;font-size:1rem}.card header p{margin:.25rem 0 0;color:#64748b;font-size:.68rem;line-height:1.45}.loading,.empty{padding:1rem;color:#64748b}.badge{border-radius:999px;background:#eff6ff;color:#1d4ed8;padding:.35rem .6rem;font-size:.62rem;font-weight:900}dl{display:grid;grid-template-columns:1fr 1fr;margin:0}dl div{padding:.8rem 1rem;border-bottom:1px solid #f1f5f9}dt{font-size:.58rem;color:#64748b;font-weight:900;text-transform:uppercase}dd{margin:.2rem 0 0;font-size:.75rem;line-height:1.4}.spend{display:grid;grid-template-columns:repeat(2,1fr);gap:.6rem;padding:1rem}.spend article{border-radius:12px;background:#f8fafc;padding:.75rem}.spend strong,.spend span,.spend small{display:block}.spend span,.spend small{margin-top:.15rem;color:#64748b;font-size:.64rem}.documents article,.rows article{display:flex;justify-content:space-between;gap:1rem;padding:.85rem 1rem;border-top:1px solid #f1f5f9}.documents strong,.documents span,.documents small,.rows strong,.rows span,.rows small{display:block}.documents span,.documents small,.rows span,.rows small{margin-top:.15rem;color:#64748b;font-size:.65rem}.documents .warn{color:#b45309}.doc-actions{display:flex;flex-wrap:wrap;justify-content:flex-end;align-items:center;gap:.35rem}.doc-actions b{border-radius:999px;background:#f1f5f9;color:#475569;padding:.28rem .5rem;font-size:.58rem}.doc-actions b.good{background:#ecfdf5;color:#047857}.doc-actions button,.reject-box button{min-height:34px;border:1px solid #cbd5e1;border-radius:8px;background:#fff;padding:0 .55rem;font-weight:800;font-size:.65rem}.reject-box{display:grid;gap:.6rem;border-top:1px solid #e2e8f0;background:#fffbeb;padding:1rem}.reject-box input{min-height:42px;border:1px solid #d6d3d1;border-radius:9px;padding:.6rem;font:inherit}.reject-box div{display:flex;gap:.4rem}.danger{background:#9f1239!important;color:#fff!important;border-color:#9f1239!important}.bookings article{align-items:center}.money{text-align:right}.consent-log{margin-bottom:2rem}@media(min-width:800px){.page{padding:1.5rem}.stats{grid-template-columns:repeat(4,1fr)}.grid{grid-template-columns:1fr 1fr}}@media(max-width:700px){.top{display:block}.top button{width:100%;margin-top:.8rem}.documents article,.rows article{display:block}.doc-actions{justify-content:flex-start;margin-top:.65rem}.money{text-align:left;margin-top:.5rem}dl{grid-template-columns:1fr}.spend{grid-template-columns:1fr}}` ]})
export class AdminCustomerDetailComponent implements OnInit{
  readonly service=inject(CustomerAdminDetailService);private readonly route=inject(ActivatedRoute);private readonly toast=inject(ToastService);readonly rejecting=signal<CustomerDocument|null>(null);rejectReason='';private userId='';
  async ngOnInit(){this.userId=this.route.snapshot.paramMap.get('userId')||'';await this.reload();}
  async reload(){try{await this.service.load(this.userId);}catch(e){this.toast.show(this.message(e),'error');}}
  verifiedDocumentCount(){return this.service.documents().filter(d=>d.verification_status==='VERIFIED').length;}
  completedCount(){return this.service.bookings().filter(b=>b.status==='COMPLETED').length;}
  activeConsent(){return this.service.consents().some(c=>!c.revoked_at);}
  address(){const p=this.service.profile();return[p?.address_line,p?.district,p?.city,p?.country,p?.postal_code].filter(Boolean).join(', ')||'-';}
  async viewDocument(doc:CustomerDocument){try{const url=await this.service.documentUrl(doc);window.open(url,'_blank','noopener,noreferrer');}catch(e){this.toast.show(this.message(e),'error');}}
  async review(doc:CustomerDocument,status:'PENDING'|'VERIFIED'|'REJECTED'|'EXPIRED'){try{await this.service.reviewDocument(doc.id,status);this.toast.show('Belge durumu güncellendi.','success');}catch(e){this.toast.show(this.message(e),'error');}}
  beginReject(doc:CustomerDocument){this.rejectReason='';this.rejecting.set(doc);}
  async confirmReject(){const doc=this.rejecting();if(!doc||!this.rejectReason.trim())return;try{await this.service.reviewDocument(doc.id,'REJECTED',this.rejectReason.trim().slice(0,500));this.rejecting.set(null);this.rejectReason='';this.toast.show('Müşteriden belgeyi yeniden yüklemesi istendi.','success');}catch(e){this.toast.show(this.message(e),'error');}}
  tierLabel(t?:string|null){return({MEMBER:'Üye',SILVER:'Silver',GOLD:'Gold',PLATINUM:'Platinum'} as Record<string,string>)[t||'MEMBER']||'Üye';}
  documentLabel(type:string){return({IDENTITY_FRONT:'Kimlik ön yüz',IDENTITY_BACK:'Kimlik arka yüz',DRIVING_LICENSE_FRONT:'Ehliyet ön yüz',DRIVING_LICENSE_BACK:'Ehliyet arka yüz',PASSPORT:'Pasaport',ADDRESS_DOCUMENT:'Adres belgesi',OTHER:'Diğer belge'} as Record<string,string>)[type]||type;}
  statusLabel(status:string){return({PENDING:'Kontrol bekliyor',VERIFIED:'Doğrulandı',REJECTED:'Yeniden isteniyor',EXPIRED:'Süresi doldu'} as Record<string,string>)[status]||status;}
  rewardLabel(type:string){return({RENTAL:'İlk araç kiralama ödülü',VEHICLE_PURCHASE:'İlk araç satın alma ödülü',TOUR:'İlk tur ödülü'} as Record<string,string>)[type]||type;}
  typeLabel(type:string){return({RENTAL:'Araç kiralama',SALE_INQUIRY:'Araç satın alma',TOUR:'Tur',APPOINTMENT:'Randevu'} as Record<string,string>)[type]||type;}
  fileSize(bytes:number){if(bytes<1024)return `${bytes} B`;if(bytes<1024*1024)return `${Math.round(bytes/1024)} KB`;return `${(bytes/1024/1024).toFixed(1)} MB`;}
  private message(e:unknown){return e instanceof Error?e.message:'İşlem tamamlanamadı.';}
}
