import { Injectable, inject, signal } from '@angular/core';
import { AuthService } from './auth.service';
import { SUPABASE_PROJECT_URL } from '../supabase.config';
export type RemoteCommand='LOCK'|'UNLOCK'|'HORN'|'IMMOBILIZE_NEXT_STOP'|'CLEAR_IMMOBILIZER';
export interface TelematicsDevice{id:string;vehicle_id:string;provider:string;external_vehicle_id?:string|null;device_id?:string|null;capabilities:Record<string,boolean>;connection_status:string;last_seen_at?:string|null;latitude?:number|null;longitude?:number|null;speed_kph?:number|null;ignition_on?:boolean|null;odometer_km?:number|null;battery_voltage?:number|null;geofence_enabled:boolean;}
export interface TelematicsVehicle{id:string;stock_code?:string|null;brand:string;model:string;model_year?:number|null;availability_status?:string|null;cover_image?:string|null;}
@Injectable({providedIn:'root'}) export class TelematicsService{private readonly auth=inject(AuthService);readonly devices=signal<TelematicsDevice[]>([]);readonly vehicles=signal<TelematicsVehicle[]>([]);readonly commands=signal<any[]>([]);readonly bridgeConfigured=signal(false);readonly loading=signal(false);private async headers(){const token=await this.auth.getAccessToken();if(!token)throw new Error('ADMIN_SESSION_REQUIRED');return{authorization:`Bearer ${token}`,'content-type':'application/json'};}async refresh(){this.loading.set(true);try{const r=await fetch(`${SUPABASE_PROJECT_URL}/functions/v1/telematics-admin`,{headers:await this.headers()});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.code||'TELEMATICS_READ_FAILED');this.devices.set(d.devices||[]);this.vehicles.set(d.vehicles||[]);this.commands.set(d.commands||[]);this.bridgeConfigured.set(d.bridgeConfigured===true);}finally{this.loading.set(false);}}deviceFor(vehicleId:string){return this.devices().find(d=>d.vehicle_id===vehicleId);}async configure(input:{vehicleId:string;provider:string;externalVehicleId:string;deviceId:string;capabilities:Record<string,boolean>}){return this.post({action:'configure_device',...input});}async command(vehicleId:string,command:RemoteCommand,reason:string){return this.post({action:'command',vehicleId,command,reason});}private async post(body:Record<string,unknown>){const r=await fetch(`${SUPABASE_PROJECT_URL}/functions/v1/telematics-admin`,{method:'POST',headers:await this.headers(),body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.code||'TELEMATICS_ACTION_FAILED');return d;}}
